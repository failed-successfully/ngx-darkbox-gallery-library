name: "Pull Request Labeler"
on: [pull_request]

jobs:
  label-pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@e54d1c08bb51b1826c8da4b05eb3fbfbc5cc4943 # renovate: tag=v3
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
          sync-labels: true
