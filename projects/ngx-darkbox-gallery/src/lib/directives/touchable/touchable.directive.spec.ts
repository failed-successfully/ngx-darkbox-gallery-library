import { TouchableDirective } from './touchable.directive';

describe('TouchableDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchableDirective();
    expect(directive).toBeTruthy();
  });
});
